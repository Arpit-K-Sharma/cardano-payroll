r # Docker-specific configuration
# This file is automatically loaded when using spring-boot.run.profiles=docker

# Database Configuration for Docker
spring.datasource.url=jdbc:postgresql://postgres:5432/cardano_payroll
spring.datasource.username=postgres
spring.datasource.password=root

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# Company wallet configuration (from environment variables)
COMPANY_WALLET_ADDRESS=${COMPANY_WALLET_ADDRESS:addr_test1vpyjcw5rrlgrpq7ry9c2z2frnsaxccd63nthac4ckenfpzc89shfw}

# Blockfrost API Configuration
BLOCKFROST_PROJECT_ID=${BLOCKFROST_PROJECT_ID}
BLOCKFROST_BASE_URL=${BLOCKFROST_BASE_URL:https://cardano-preprod.blockfrost.io/api/v0}

# Company private key (from environment variable)
COMPANY_SKEY=${COMPANY_SKEY}

# Scripts path in Docker container
BATCH_SCRIPT_PATH=${BATCH_SCRIPT_PATH:/app/scripts/send-ada.js}

# Logging Configuration
logging.level.org.example.cardanopayroll=INFO
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=DEBUG

# Server Configuration
server.port=8080
server.servlet.context-path=/


# Management endpoints for health checks
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always
